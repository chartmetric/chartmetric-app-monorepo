# FontAwesome Pro registry mapping only — the auth token is NEVER in this repo.
# pnpm ignores credentials in project-level .npmrc by design; set the token in
# user-level config once per machine (see README -> Private registry auth).
@fortawesome:registry=https://npm.fontawesome.com/
