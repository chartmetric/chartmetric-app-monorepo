# machine-generated — pnpm owns this file's formatting
pnpm-lock.yaml

# Vendored agent skills (managed by `npx skills`)
.agents/skills/

# Machine-generated API contracts
apps/api/openapi.generated.json
packages/api-client/src/schema.generated.ts

# Harness phase state — the runner rewrites these mid-pipeline
phases/

# Harness git worktrees (local-only, ignored via .git/info/exclude)
.worktrees/
