# Phase NN — <title>

<!-- This file is the writer agent's prompt, sent verbatim on every
     retry attempt. Copy to phases/NN-<slug>.md. The lint stage
     requires an `## Acceptance` section whose bullet count matches
     the JSON `acceptance` array. -->

## Goal

<!-- One paragraph: what exists at the end of this phase that doesn't
     exist now, and why. -->

## Acceptance

<!-- Mirror the JSON `acceptance` array, one bullet each. Criteria must
     be mechanically checkable — the verification_cmd and gates should
     be able to prove them. -->

- criterion 1
- criterion 2

## In scope

<!-- Files/areas the writer may touch. Mirror the JSON `files` list. -->

## Out of scope

<!-- Explicit boundaries. Name adjacent work the writer must NOT do —
     scope creep here is the most common writer failure. -->

## Notes / open questions

<!-- Relevant ADR numbers, architecture sections, gotchas from prior
     retros, known blockers outside this repo. -->
