{
  "id": "06-influencers-page-filters",
  "title": "wire the influencer filter panel to the list query",
  "status": "completed",
  "depends_on": [
    "04-influencer-filter-options-api",
    "05-influencers-page-list"
  ],
  "acceptance": [
    "the page renders category, country, gender and age-group filters populated from the filter-options route",
    "a handle search input filters the list, debounced so rapid keystrokes collapse into one query",
    "applying a filter resets the offset to zero and refetches, and the result count reflects the filtered total",
    "included and excluded selections both reach the API, using the existing include/exclude query parameter pairs",
    "country filter options display locale-correct names while sending ISO codes to the API",
    "the age-group filter offers only the six supported buckets",
    "a filter-options request failure degrades to a dismissible warning without breaking the list",
    "new strings are extracted and translated across all seven locales so lingui compile --strict passes"
  ],
  "files": [
    "apps/web/src/pages/creators/influencers/**",
    "apps/web/src/locales/creators/**"
  ],
  "gates": [
    "pnpm lint",
    "pnpm typecheck",
    "pnpm test",
    "pnpm --filter web exec lingui compile --strict"
  ],
  "verification_cmd": "pnpm --filter web exec vitest run InfluencersPage.filters",
  "security_review": false,
  "notes": "Completes the PRD entry. Behavioural parity with the artists filter panel (ArtistFilters, ArtistFiltersDrawer, artist-filter-draft) is an acceptance concern, not a stylistic one.",
  "attempts": 1,
  "run_started_at": "2026-08-10T17:40:33+00:00",
  "stage": "commit",
  "out_of_scope_files": [],
  "review_findings": "BACKLOG_WORTHY:\n- FilterOptionsError warning offers only Retry, not a close/dismiss \u2014 acceptance says 'dismissible'; shared with ArtistsPage precedent, so fix both together (low)\n- Handle search is debounced twice (HandleSearchInput 350ms \u2192 applyDraft 350ms \u2248 700ms), matching the artists precedent; consider single-debouncing across both filter panels (low)"
}
