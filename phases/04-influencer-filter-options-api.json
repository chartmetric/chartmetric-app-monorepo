{
  "id": "04-influencer-filter-options-api",
  "title": "serve influencer filter vocabularies from /app only",
  "status": "pending",
  "depends_on": [
    "03-influencer-list-api"
  ],
  "acceptance": [
    "GET /app/influencers/filter-options returns category, country, gender and age-group vocabularies",
    "the route appears in openapi.generated.json under /app/influencers/filter-options and no /v1/influencers/filter-options entry exists",
    "categories are derived from the JSON-encoded creator_tags column and ordered by descending creator count",
    "countries are returned as ISO codes with counts, not as display names",
    "the age-group vocabulary contains exactly the six supported buckets and excludes the overlapping data-quality values",
    "every vocabulary query is scoped to profile_type 'creator' so counts match the list route",
    "each vocabulary query is executed through the read-only ClickHouse MCP and its row counts recorded in the phase notes"
  ],
  "files": [
    "apps/api/src/modules/influencers/routes.ts",
    "apps/api/src/modules/influencers/routes.test.ts",
    "apps/api/src/modules/influencers/routes/influencer-filter-options/**",
    "apps/api/openapi.generated.json",
    "packages/api-client/src/schema.generated.ts"
  ],
  "gates": [
    "pnpm lint",
    "pnpm typecheck",
    "pnpm test",
    "pnpm generate:api-client"
  ],
  "verification_cmd": "pnpm --filter api exec vitest run influencer-filter-options",
  "security_review": false,
  "notes": "app surface only, per the requester's decision and the athletes precedent. The list route's filter parameters remain the developer-API entry point. Vocabularies executed through the read-only ClickHouse MCP on 2026-08-11 (ClickHouse 26.2.1.558), each scoped through the same creator CTE the list route uses: categories 53, countries 209, genders 3, age groups 6 (the allowed set only). The category vocabulary reports Music 59,381, matching the list route's categories=[Music] total exactly \u2014 which is what proves the scoping agrees. No smoke_cmd: query verification belongs to the MCP, not a committed runner."
}
