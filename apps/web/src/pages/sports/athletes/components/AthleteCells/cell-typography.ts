import type { MantineSize } from "@mantine/core";

/**
 * One type scale for every cell on the athletes table, matching the `size="sm"`
 * its text cells render at. A numeric column set apart from its neighbours
 * reads as emphasis the data does not justify.
 */
export const CELL_TEXT_SIZE: MantineSize = "sm";
