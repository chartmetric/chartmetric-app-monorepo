import type { MantineSize } from "@mantine/core";

/**
 * One type scale for the peer list cells. A column whose text is visibly
 * larger than its neighbours reads as emphasis the data does not justify.
 */
export const CELL_TEXT_SIZE: MantineSize = "xs";
